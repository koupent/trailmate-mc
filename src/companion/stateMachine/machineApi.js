/**
 * ESM-friendly re-exports for mineflayer-statemachine (CJS package).
 */
import sm from 'mineflayer-statemachine';

export const {
    StateTransition,
    NestedStateMachine,
    BotStateMachine,
    BehaviorIdle
} = sm;
